version: 1.0.0
events:
  build:
    steps:
      -
        try:
          type: script
          script:
            - 'set -x'
            - 'ls -la ~/.ssh'
            - cat ~/.ssh/config
            - cd /tmp
            - git clone git@github.com:bjaspan/goofy.git
