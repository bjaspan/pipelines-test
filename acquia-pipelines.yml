version: 1.0.0
events:
  build:
    steps:
      - js:
          type: script
          script:
            - date > build.log
            - nvm install 4.4.1
            - nvm use 4.4.1
            - npm --version
            - node --version
            - npm install
